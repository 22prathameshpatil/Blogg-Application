<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <%- include('./partials/scripts') %> <%- include('./partials/navbar') %>
    <div class="bg-gray-100 flex justify-center items-center">
      <form
        action="/blog"
        method="post"
        class="bg-white p-8 rounded-lg shadow-lg w-[1000px] mt-4"
        enctype="multipart/form-data"
      >
        <h2 class="text-3xl font-bold mb-6 text-center">Add New Blog</h2>

        <!-- Blog Title -->
        <label class="block mb-2 font-semibold text-lg" for="title"
          >Title:</label
        >
        <input
          type="text"
          id="title"
          name="title"
          required
          class="w-full px-4 py-1 border rounded-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
        />

        <label class="block mb-2 font-semibold text-lg mt-4" for="title"
          >Cover Image:</label
        >
        <input
          type="file"
          id="converImageUrl"
          name="converImageUrl"
          required
          class="w-full px-4 py-1 border rounded-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
        />

        <!-- Blog Body -->
        <label class="block mt-6 mb-2 font-semibold text-lg" for="body"
          >Body:</label
        >
        <textarea
          id="body"
          name="body"
          rows="5"
          required
          class="w-full px-4 py-1 border rounded-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
        ></textarea>

        <!-- Submit Button -->
        <button
          type="submit"
          class="w-full bg-blue-500 text-white py-3 mt-6 rounded-lg text-lg font-semibold hover:bg-blue-600 transition"
        >
          Publish Blog
        </button>
      </form>
    </div>
  </body>
</html>
