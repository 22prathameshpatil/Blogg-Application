<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Blog</title>
  </head>
  <body class="bg-gray-100 p-1">
    <%- include('./partials/scripts') %>
    <%- include('./partials/navbar') %>

    <div class="max-w-4xl mx-auto bg-white p-8 rounded-lg shadow-lg">
      <h2 class="text-3xl font-bold mb-6 text-center">Add New Blog</h2>

      <form action="/blog" method="post" enctype="multipart/form-data">
        <!-- Blog Title -->
        <label class="block mb-2 font-semibold text-lg" for="title">Title:</label>
        <input type="text" id="title" name="title" required
          class="w-full px-4 py-1 border rounded-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-400" />

        <label class="block mb-2 font-semibold text-lg mt-4" for="coverImageUrl">Cover Image:</label>
        <input type="file" id="coverImageUrl" name="coverImageUrl" required
          class="w-full px-4 py-1 border rounded-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-400" />

        <!-- Blog Body -->
        <label class="block mt-6 mb-2 font-semibold text-lg" for="body">Body:</label>
        <textarea id="body" name="body" rows="5" required
          class="w-full px-4 py-1 border rounded-lg text-lg focus:outline-none focus:ring-2 focus:ring-blue-400"></textarea>

        <!-- Submit Button -->
        <button type="submit"
          class="w-full bg-blue-500 text-white py-3 mt-6 rounded-lg text-lg font-semibold hover:bg-blue-600 transition">
          Publish Blog
        </button>
      </form>
    </div>
  </body>
</html>
